name: Pa11y CI

on: [push]

jobs:
  pa11y:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      
      - name: Run Pa11y
        run: npm install pa11y-ci
        run: npm pa11y-ci
